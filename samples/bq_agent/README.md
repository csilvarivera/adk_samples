# BigQuery Agent

An agent that interacts with BigQuery to perform data analysis and queries.

## Setup

1. **Install Dependencies**:
   ```bash
   pip install -r requirements.txt
   ```

## Running the Agent

```bash
python -m bq_agent
```
