# Auto Insurance Agent

This agent is designed to handle auto insurance claims and queries using the ADK.

## Setup

1. **Install Dependencies**:
   ```bash
   pip install -r requirements.txt
   ```

2. **Configure Environment**:
   Ensure `.env` is configured with your project details.

## Running the Agent

```bash
python -m auto_insurance_agent
```
